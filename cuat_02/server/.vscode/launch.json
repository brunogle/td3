{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/bin/server",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "external"
        }
    ]
}
