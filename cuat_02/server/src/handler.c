#include <stdio.h>

#include <unistd.h>

#include "handler.h"
#include "buffer.h"


void handler_proc(event_buffer_t * buffer){

    /*
    
    abrir lcd

    while(1){
        maquina de estados
    }
    
    */

    while(1){

        //

        sleep(1);
        

    }
}